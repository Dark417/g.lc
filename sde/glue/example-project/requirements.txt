boto3>=1.34.0
botocore>=1.34.0

# Testing (offline AWS mocks)
pytest>=8.0.0
moto[glue]>=5.0.0

# Note: awsglue (GlueContext, DynamicFrame) and pyspark are provided by the
# AWS Glue runtime. They are NOT installed here because the ETL job in
# etl/glue_job.py runs on Glue workers, not locally. For local Glue dev use the
# official AWS Glue Docker image (amazon/aws-glue-libs).
